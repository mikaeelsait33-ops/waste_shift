{
  "name": "wasteshift",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "test": "node scripts/waste-calculations.test.mjs && node scripts/invoice-parsing.test.mjs",
    "firebase:smoke": "node scripts/firebase-smoke.mjs",
    "seed:database": "node scripts/seed-vercel-database.mjs",
    "preview": "vite preview",
    "firebase:emulators": "npx firebase-tools emulators:start --only firestore,hosting",
    "firebase:deploy:firestore": "npx firebase-tools deploy --only firestore",
    "firebase:deploy:hosting": "npm run build && npx firebase-tools deploy --only hosting"
  },
  "dependencies": {
    "@vercel/blob": "^2.5.0",
    "firebase": "^12.15.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "recharts": "^3.9.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "oxlint": "^1.69.0",
    "vite": "^8.1.0"
  }
}
